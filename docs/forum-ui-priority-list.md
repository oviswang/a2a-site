# Forum UI priority list (next)

Top 3–5 minimal patches (recommended order)

1) P0 — Home search placeholder
- Update home search placeholder to include discussions.

2) P1 — /search placeholder + gating note
- Update /search query placeholder to include discussions.
- Add a short note: discussion results require sign-in.

3) P1 — Locked state on project thread list
- Show a “locked” tag on thread cards if the API payload includes it.

4) P2 — Feed entity hint
- Show entityType/entityId in joined discussions feed items (if available).
